# 🚀 Paqet Auto Installer

اسکریپت نصب و مدیریت خودکار **Paqet** برای سرورهای لینوکس (Ubuntu/Debian).
با این ابزار می‌توانید بدون درگیر شدن با تنظیمات پیچیده، مک‌آدرس یا فایروال، پروکسی خود را راه اندازی کنید.

This script automates the installation and configuration of the Paqet proxy server on Linux.

---

## 📥 نصب سریع (Quick Install)

برای نصب، کافیست دستور زیر را در ترمینال سرور خود کپی و اجرا کنید:

```bash
bash <(curl -Ls [https://raw.githubusercontent.com/Alexander-McQuen/paqet-installer/main/paqet.sh](https://raw.githubusercontent.com/Alexander-McQuen/paqet-installer/main/paqet.sh))



✨ ویژگی‌ها (Features)
✅ نصب خودکار پیش‌نیازها (libpcap, net-tools, etc)

✅ تشخیص هوشمند آی‌پی سرور و مک‌آدرس روتر (Gateway MAC)

✅ مدیریت پورت (جلوگیری از تداخل با پورت ۴۴۳)

✅ منوی مدیریت (نصب، حذف، آپدیت، نصب مجدد)

✅ تنظیم فایروال (iptables) به صورت خودکار

✅ اجرا به عنوان سرویس (Systemd Service) - با ریستارت سرور قطع نمی‌شود

⚙️ راهنما (How to use)
۱. با کاربر root وارد سرور شوید. ۲. کد نصب بالا را اجرا کنید. ۳. از منوی ظاهر شده، گزینه 1 (Install) را انتخاب کنید. ۴. پورت مورد نظر خود را وارد کنید (پیش‌فرض: 8443). ۵. اسکریپت تمام کارها را انجام داده و در انتها IP و Secret Key را به شما نمایش می‌دهد.

🗑 حذف (Uninstall)
برای حذف کامل برنامه، دوباره دستور نصب را اجرا کنید و از منو گزینه 2 (Uninstall) را انتخاب کنید

### مرحله ۲: ذخیره کردن
بعد از اینکه متن بالا را در فایل `README.md` گذاشتی، برو پایین صفحه و دکمه سبز **Commit changes** را بزن.

### نتیجه
حالا اگر به صفحه اصلی مخزنت برگردی، می‌بینی که گیت‌هاب آن کدها را به صورت خوشگل و فرمت‌بندی شده نشان می‌دهد و کاربران می‌توانند با زدن دکمه کپی (که گوشه کادر سیاه کد می‌آید)، دستور نصب را بردارند.

**نکته:** من لینک را بر اساس نام کاربری `Alexander-McQuen` که در عکس فرستادی تنظیم کردم. اگر دقیقاً همین است، کد کار می‌کند.

